export interface Project {
  id: string;
  title: string;
  category: string;
  image: string;
  projectLink?: string;
}

export interface Service {
  icon: string;
  title: string;
  description: string;
  color: string;
}
